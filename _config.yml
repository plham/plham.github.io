url: 'http://izumi-lab.github.io'
title: 'Plham documentation'

include: ['tutorial', 'class']

markdown: kramdown

kramdown:
  input: GFM
  syntax_highlighter_opts:
    disable : true

highlighter: null

defaults:
  -
    scope:
      path: ""
    values:
      layout: default
      math: true
